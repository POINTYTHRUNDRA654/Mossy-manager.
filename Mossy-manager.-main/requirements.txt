# Core dependencies for Mossy Manager
# MO2 Load Order Manager, Conflict Resolution, and Patching Tool

# Configuration and data handling
pyyaml>=6.0
toml>=0.10.2
configparser>=5.3.0

# CLI interface
click>=8.1.0
colorama>=0.4.6
tabulate>=0.9.0

# Local web UI
pydantic>=2.8.0
fastapi>=0.115.0
uvicorn>=0.30.0
httpx>=0.27.0

# AI brain — machine learning (free, open-source)
scikit-learn>=1.3.0
numpy>=1.24.0

# For building executables (optional - install with: pip install -e ".[build]")
pyinstaller>=5.13.0

# Development / testing (optional - install with: pip install -e ".[dev]")
pytest>=7.4.0
pytest-cov>=4.1.0
